#include<stdio.h>
#include<conio.h>
void main()
{
	int no, rev=0,reminder;
	clrscr();
	printf("\n Enter the Numbers:");
	scanf("%d",&no);
	while(no!=0)
	{
		reminder = no % 10;
		rev = rev * 10 + reminder;
		no = no / 10;
	}
	printf("\n Revserse Number=  %d",rev);
	getch();


}